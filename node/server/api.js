'use strict';

module.exports = {
  createApi: (url, req, res) => {
    l.log(`${req.connection.remoteAddress} connected to API server!`);

      switch (url) {
        case ('/'):
          return new Promise((res, rej) => {

          });
        case ('/'):
          return new Promise((res, rej) => {

          });
        case ('/'):
          return new Promise((res, rej) => {

          });
        case ('/'):
          return new Promise((res, rej) => {

          });
        default:
          //...
      }
  }
};